Finance Data Processing and Access Control Backend.

Node.js, Express, TypeScript, Prisma, and SQLite.

Run it with:

```bash
npm install
npm run prisma:generate
npm run dev
```

API notes are in [API.md](API.md).
